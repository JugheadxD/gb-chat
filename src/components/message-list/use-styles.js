import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() => {
  return {
    input: {
      color: "#9a9fa1",
      padding: "10px 15px",
      fontSize: "15px",
    },
    icon: {
      color: "rgba(21, 70, 160, 0.76)",
      cursor: "pointer",
    },
  };
});
